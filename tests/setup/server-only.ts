// Stub do pacote `server-only` para os testes.
// O pacote real lança erro quando importado fora de um Server Component;
// nos testes (ambiente node) só precisamos que o import seja inofensivo.
export {};
